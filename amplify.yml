version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - npm run build
                build:
                    commands: [""]
      artifacts:
        baseDirectory: .next
        files:
            - '**/*'
      cache:
        paths:
          - node_modules/**/*
    appRoot: /frontend
